-- A script lists all records of secong_table in the database hbtn_0c_0
-- Results should display the score, then the name.
-- The score should be in descending order.

SELECT score,
name FROM second_table
ORDER BY score DESC;
